# Frontend (Vite) configuration for local dev.
# API base path — left relative so the Vite proxy / nginx routes it.
VITE_API_BASE=/api
# Where the dev proxy forwards /api during `npm run dev`.
VITE_PROXY_TARGET=http://localhost:4000
