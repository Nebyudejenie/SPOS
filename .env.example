# Root environment for docker-compose. Copy to .env and adjust as needed.
# Database
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=appdb
DB_PORT=5432

# Backend
BACKEND_PORT=4000
LOG_LEVEL=info
CORS_ORIGIN=*

# Frontend (host port -> container :80)
FRONTEND_PORT=8080
